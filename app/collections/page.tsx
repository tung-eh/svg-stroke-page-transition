export default function Collections() {
  return (
    <div className="w-full">
      <header className="h-screen w-full flex items-center justify-center font-condensed font-extrabold text-[clamp(5rem,15vw,10rem)] text-center -tracking-[2%] leading-none py-60 px-8">
        The Hover State
      </header>
      <footer className="h-screen w-full flex items-center justify-center font-condensed font-extrabold text-[clamp(5rem,15vw,10rem)] text-center -tracking-[2%] leading-none py-60 px-8">
        End of Interaction
      </footer>
    </div>
  )
}
